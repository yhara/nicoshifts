#!/usr/bin/env ruby
$LOAD_PATH.unshift File.expand_path("../lib", File.dirname(__FILE__))
require 'nicoshifts'
require 'sinatra'
require 'slim'

conf = NicoShifts::Conf.get
$nv = Nicovideo.new(conf['mail'], conf['password'])

NicoShifts::Server.run! :port => 2525
